.sound-settings-screen-container {
    flex-grow: 1;
    min-width: auto;
    min-height: auto;
    background-color: rgba(0, 0, 0, 0);
}

.volume-slider {
    min-height: auto;
    color: #ffffff;
}

.volume-label {
    width: 50px;
    padding-left: 5px;
}

.play-sfx-button {
  width: 100%;
}
